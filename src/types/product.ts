export type Product = {
    image: string
    name: string
    prevPrice: number
    currentPrice: number
    freeShipping: boolean
    installments: number
    id: string
    description: string
    detailsLink: string
}